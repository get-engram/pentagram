{
  "name": "pentagram-hosted",
  "private": true,
  "version": "0.1.0",
  "description": "Hosted pentagram: one Durable Object per tenant, MCP over HTTP, Workers AI embeddings.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260702.1",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}