// Optional dependencies are dynamically imported and may be absent at
// runtime; declare them untyped so the build never depends on their presence.
declare module "hyparquet-writer";
declare module "hyparquet";
